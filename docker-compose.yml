version: '2'

services:

### Sync Server ###########################
    sync-server:
        build:
            context: ./docker/sync-server
        working_dir: /go/bin
        command: "sync-wp-repo"